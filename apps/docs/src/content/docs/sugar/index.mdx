---
title: Sugar
description: Convenience helpers for common patterns
---

## What

`@mantaq/sugar` wraps core primitives with convenience helpers. Every helper
compiles to the same behavior you could write yourself.

## Helpers

| Helper                                           | Purpose                     |
| ------------------------------------------------ | --------------------------- |
| `states()`, `events()`                           | Batch create states/events  |
| `matches()`, `isIn()`, `activeLeaves()`, `tag()` | State matching and grouping |
| `ActorMap`, `broadcast()`                        | Dynamic child management    |
| `withPromise()`, `withTimeout()`                 | Effect helpers              |
| `actorSpec`, `definePart`, `withParts`           | Split setup across files    |

## Install

```bash
npm install @mantaq/sugar
```

Requires `@mantaq/core`. Single entry point:

```ts
import { matches, states, events } from "@mantaq/sugar";
```

## When to use

Use sugar when many states/events, frequent state checks, dynamic children,
or promise effects. Skip when few states, minimal checks, no dynamic children,
or custom effect patterns.

Core is sufficient for everything. Sugar saves typing.

## Pages

- [Batch Creation](/mantaq/sugar/batch-creation/): `states()` and `events()`
- [Matching](/mantaq/sugar/matching/): `matches()`, `isIn()`, `activeLeaves()`, `tag()`
- [Composition](/mantaq/sugar/composition/): `actorSpec`, `definePart`, `withParts`
- [Dynamic Children](/mantaq/sugar/dynamic-children/): `ActorMap` and `broadcast()`
- [Effect Helpers](/mantaq/sugar/effect-helpers/): `withPromise()` and `withTimeout()`
- [Request / Response](/mantaq/sugar/request-response/): short-lived request handlers

# @mantaq/traversal

Graph extraction, traversal algorithms, and runtime tracking for
[@mantaq/core](https://www.npmjs.com/package/@mantaq/core) actors.

Build the state-machine graph from an actor, walk it with traversal
algorithms, and instrument coverage as the actor runs — useful for
verifying that every declared transition is reachable and exercised.

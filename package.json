{
  "name": "mantaq",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "ready": "vp check && vp run guard && vp run -r test && vp run -r build",
    "guard": "node scripts/vision-guard.mjs",
    "dev": "vp run docs#dev",
    "prepare": "vp config"
  },
  "devDependencies": {
    "@varlock/bumpy": "catalog:",
    "vite": "catalog:",
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@11.5.0"
}
